#pragma once
#include "Level.h"

// This class is a hard coded instance of a Level that is being used because we didn't have time to make
// a level editor
class ArenaLevel: public Level
{
public:
    ArenaLevel();
    ~ArenaLevel();
};

